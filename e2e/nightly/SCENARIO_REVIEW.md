# Scenario review before implementation

> Current scope (Owner decision 2026-09-24): **37 active executions, 6 TODO, 2 withdrawn**. Product fixes are deferred. See [DEFERRED_TESTS.md](DEFERRED_TESTS.md) for reasons and explicit reproduction. Earlier failure results below are historical and remain unchanged.

Source: Owner archive SHA-256 be4803e5c7aefa68458e33e6b3c9cd96dafa144c372a4c9601e43583dbb140dc. All baseline cases NOT RUN for this task. Original finding claims are historical only. Latest nightly selections and available git commits are in evidence/registry.json. No upstream repository required. Initial extraction log said42 files; actual inventoried source count is41.

| Group / scenario | Preconditions and components | Expected result | Cleanup | Status / evidence |
| --- | --- | --- | --- | --- |
| regressions: An interrupted save_file preserves the existing file | MCP in-memory transport or injected Codex WebSocket | Feature Then assertions; see `tests/component/regressions.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| regressions: A Codex initialize timeout closes its WebSocket | MCP in-memory transport or injected Codex WebSocket | Feature Then assertions; see `tests/component/regressions.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| flows: An invitation, messages, and a file travel from <sender> to <receiver> | Two daemon roots, remote SDK, local broker | Feature Then assertions; see `tests/features/flows.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| flows: A public invitation can be revoked | Two daemon roots, remote SDK, local broker | Feature Then assertions; see `tests/features/flows.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| flows: A second client cannot take a busy identity without explicit force | Two daemon roots, remote SDK, local broker | Feature Then assertions; see `tests/features/flows.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| flows: Server state is isolated | Two daemon roots, remote SDK, local broker | Feature Then assertions; see `tests/features/flows.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| regressions-client: A remote client does not send its credential over HTTP | Remote SDK and authenticated REST | Feature Then assertions; see `tests/features/regressions-client.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| regressions-client: The server limits JSON request size | Remote SDK and authenticated REST | Feature Then assertions; see `tests/features/regressions-client.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| regressions-fleet: Fleet launches two task agents into one Cowork room | Two Cowork services, Fleet pair template and ACP mock | Feature Then assertions; see `tests/features/regressions-fleet.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| regressions-telegram: A corrupt bot registry is not overwritten | Connector CLI and isolated registry/unit/mock fixtures | Feature Then assertions; see `tests/features/regressions-telegram.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| regressions-telegram: A second serve process does not hide the running connector from CLI status | Connector CLI and isolated registry/unit/mock fixtures | Feature Then assertions; see `tests/features/regressions-telegram.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| regressions-telegram: A systemd unit preserves a path with spaces as one value | Connector CLI and isolated registry/unit/mock fixtures | Feature Then assertions; see `tests/features/regressions-telegram.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| regressions-telegram: A lost Telegram response does not duplicate an outgoing message | Connector CLI and isolated registry/unit/mock fixtures | Feature Then assertions; see `tests/features/regressions-telegram.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| rooms-agents: A remote client joins a Cowork room and exchanges messages | Cowork on both servers; Fleet/ACP mock for launch cases | Feature Then assertions; see `tests/features/rooms-agents.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| rooms-agents: Fleet launches a task agent into a Cowork room | Cowork on both servers; Fleet/ACP mock for launch cases | Feature Then assertions; see `tests/features/rooms-agents.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| smoke: Servers have distinct instance IDs | Two credential-isolated clients and daemons | Feature Then assertions; see `tests/features/smoke.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| smoke: Private metadata requires a valid credential | Two credential-isolated clients and daemons | Feature Then assertions; see `tests/features/smoke.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| smoke: A client attaches to its assigned server | Two credential-isolated clients and daemons | Feature Then assertions; see `tests/features/smoke.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| smoke: A mismatched server instance ID is rejected | Two credential-isolated clients and daemons | Feature Then assertions; see `tests/features/smoke.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| smoke: A credential for one server cannot access the other | Two credential-isolated clients and daemons | Feature Then assertions; see `tests/features/smoke.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |
| telegram: The connector registers a bot, polls, and answers a chat command | Connector and HTTPS Telegram mock | Feature Then assertions; see `tests/features/telegram.feature` | Leases/fixture teardown where provided; disposable project volumes after group; per-scenario isolation needs correction | NOT RUN; feature and associated steps statically read |

## Risk-driven additions and deferrals

| Area | Decision and prerequisite |
| --- | --- |
| Fresh install | Existing image build is package install only; standalone installer first-run remains deferred pending installer contract and disposable VM/systemd target. |
| Upgrade / rollback / interruption | Deferred: archive declares no supported previous-version pair or rollback/migration contract. Never infer downgrade safety from npm version numbers. |
| Daemon restart / crash / readiness / persistence | Add graceful and forced process restart with preserved identity, contacts, messages, files and credentials using dedicated project; compose readiness bounded by health checks; deliberate slow-readiness injection remains unimplemented. Migration and backup/restore deferred until supported commands/contracts identified. |
| SDK / CLI / MCP | Add real MCP stdio discovery and tool invocation/error/session cleanup; existing SDK remote and CLI coverage retained. TLS and compatibility matrix deferred pending supported proxy/version matrix. |
| Identity | Add temporary owner exclusivity and release retirement, duplicate and force takeover existing. Crash stale-lease reconciliation deferred until explicit expiry contract. |
| Message / file security | Add cross-identity file/history denial and bounded unread/history cursor checks. Offline retries/replay/resource flood deferred pending delivery/idempotency guarantees; no fictional durable broker queue assertion. |
| Cowork | Correct reused-root failure; existing cross-host room membership/message smoke retained. Public docs define default-deny authenticated command grants; dedicated grant/revoke/removed-seat command tests remain unimplemented in this revision (coverage gap, not unavailable product capability). Events/files and room restart likewise remain gaps; daemon restart is not a substitute. |
| Fleet | Existing single/pair plus team and task review/finish completion added using public CLI. Targeted completion passed after launch fix and current documented deletion semantics; assertions include member identity retirement and supervisor termination. Partial provisioning and recovery failure were observed after the overlong harness socket path induced startup failure; this is diagnostic evidence, not a deterministic new partial-provisioning test. Monitor wake remains deferred: the ACP peer only acknowledges prompt text, does not authenticate/read mailbox; asserting it received mail would be false coverage. |
| HTTP | Existing auth/size regressions retained; malformed/duplicate/mixed file selections added with valid-state preservation checks; arbitrary malformed REST bodies deferred until endpoint error-schema contract is specified. Replay not claimed absent authenticated protocol contract. |
| Database | Restart state assertions added; direct corruption/concurrency/migration and restore tests deferred to documented storage contract to avoid pretending component pokes are E2E. |
| Container / systemd | Container health and teardown asserted. Existing systemd generation uses fake systemctl; not actual boot/parser validation. |
| Browser | No running browser/frontend surface in archive topology; Messenger dependency alone is not frontend E2E. Deferred until auth/bootstrap contract and fixture. |
| Harness | Fix aggregate gate, functional continuation, per-case clean project option, deadlines, cleanup failures, safe credential-free logs, unique image namespace and provenance. Add fault-injected runner verification. |
